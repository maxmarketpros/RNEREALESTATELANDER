[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Forms are handled via Netlify Functions for Next.js 14+
[[redirects]]
  from = "/api/forms/*"
  to = "/.netlify/functions/:splat"
  status = 200
